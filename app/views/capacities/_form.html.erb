<%= simple_form_for(@capacity) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :limit %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
